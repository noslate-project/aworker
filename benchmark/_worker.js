'use strict';

// MARK: - Code stub to be replaced at runtime.
function scriptToRun() {
  /** __WORKER_SCRIPTS_TO_RUN */
}
// MARK: END - Code stub to be replaced at runtime.

// MARK: - Code stub to be replaced at runtime.
/** __WORKER_HARNESS_CODE__ */
// MARK: END - Code stub to be replaced at runtime.

scriptToRun.call(globalThis);
